export class Country {
    constructor(
        public id: number,
        public country: string,
        public code: string,
        public currency: string,
        public symbol: string
    ) {
    }
}
